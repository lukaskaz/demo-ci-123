name: Trigger gitboard monitoring
  
on: 
    push:
        branches:
            - test-ci

jobs:
    build:
    	name: build
        runs-on: ubuntu-latest
	environment: production
        steps:
        - name: Checkout
	  uses: actions/checkout@v3
   	- name: Building
    	  run: |
       	    mkdir build && cd build && cmake .. && make && ./src/demo-run
